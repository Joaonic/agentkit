# 02 - Implementation

Use `implement-plan` with mandatory phase order:
1. investigation
2. TDD prep
3. implementation loop
4. technical review
5. UX review (if UI changes)
6. posttask
7. final report

Use `executing-plans-parallel` only for disjoint write scopes.
